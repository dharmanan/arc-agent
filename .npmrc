# Production surface: npm audit --omit=dev → 0 vulnerabilities
# Dev dep vulnerabilities (undici via hardhat@2 toolbox) cannot be fixed
# without upgrading to hardhat@3 (breaking change). Use audit-level=high
# to avoid false CI failures from known-unfixable transitive deps.
audit-level=high
